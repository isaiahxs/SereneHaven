import { Modal, useModalContext } from '../../context/Modal';
import './ReviewModal.css';

export default function Review({sessionUser}) {
    const {showReviewModal, setShowReviewModal} = useModalContext();

    return (
        <>
        </>
    )
}
